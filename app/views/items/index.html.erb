<%= form_tag(items_path, method: "get") do %>
  <%= select_tag "sort_by_department", options_for_select(Item.departments, @department)%>
  <%= submit_tag "Filter" %>
<% end %>

<% @items.each do |item| %>
<li>
  <%= link_to item.name, item_path(item) %>
</li>
<% end %>
