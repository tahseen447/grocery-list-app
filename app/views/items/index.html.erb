<%= form_tag(items_path, method: "get") do %>
  <%= select_tag "sort_by_department", options_for_select(["Produce", "Deli", "Frozen", "Dairy"]), include_blank: false %>
  <%= submit_tag "Filter" %>
<% end %>

<% @items.each do |item| %>
<li>
  <% link_to item %>
</li>
<% end %>
